# Aliases for Visual Studio Code.
# This requires the VS Code command line tools to be installed!

### Aliases
alias vsc='code .' # open current directory in vs code
alias vsca="code --add" # add dir to last active window
alias vscn='code --new-window' # open dir or file in new window
alias vscr='code --reuse-window' # open dir or file in last active window
alias vscd="code --diff" # compare two files with each other
alias vscw='code --wait' # open dir or file and wait for files to close
